# Codex Native Ports

This directory is reserved for the second port line:

- same logical skill coverage as `v2.2`
- rewritten natively for Codex runtime and tool semantics
- same high-level contracts where possible, but not strict-copy text

Current status: placeholder only.
